{
  "name": "infohub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build",
    "start": "cd client && npm start"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}